---
import Layout from '../layouts/Layout.astro';
import { proyectos } from '../data/proyectos';
const base = import.meta.env.BASE_URL;
---

<Layout title="Proyectos - Cristian Baeza Torres">
  <div class="space-y-12">
    <div class="space-y-2">
      <h1 class="text-4xl font-bold text-gray-900">Proyectos</h1>
      <p class="text-lg text-gray-600">Iniciativas de investigación y desarrollo</p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
      {proyectos.map((proyecto) => {
        const borderColorClass = {
          'blue': 'border-blue-600',
          'green': 'border-green-600',
          'red': 'border-red-600',
          'purple': 'border-purple-600',
        }[proyecto.color] || 'border-gray-600';
        
        return (
          <div class={`bg-white rounded-lg shadow-md hover:shadow-lg transition overflow-hidden border-l-4 ${borderColorClass}`}>
            <div class="p-6 space-y-4">
              <h3 class="text-xl font-bold text-gray-900">{proyecto.titulo}</h3>
              <p class="text-gray-700">
                {proyecto.descripcion}
              </p>
              <div class="flex flex-wrap gap-2">
                {proyecto.tags.map((tag) => {
                  const colorClasses = {
                    'Data Science': 'bg-blue-100 text-blue-800',
                    'Epidemiología': 'bg-green-100 text-green-800',
                    'Python': 'bg-purple-100 text-purple-800',
                    'NLP': 'bg-green-100 text-green-800',
                    'Machine Learning': 'bg-blue-100 text-blue-800',
                    'Liderazgo': 'bg-red-100 text-red-800',
                    'Sociedad Científica': 'bg-purple-100 text-purple-800',
                    'Gestión': 'bg-blue-100 text-blue-800',
                    'Antropología': 'bg-purple-100 text-purple-800',
                    'Redes': 'bg-orange-100 text-orange-800',
                    'Series Temporales': 'bg-indigo-100 text-indigo-800',
                    'Antropología Forense': 'bg-teal-100 text-teal-800',
                    'Bioarqueología': 'bg-cyan-100 text-cyan-800',
                  };
                  const colorClass = colorClasses[tag] || 'bg-gray-100 text-gray-800';
                  return (
                    <span class={`${colorClass} px-3 py-1 rounded text-sm`}>
                      {tag}
                    </span>
                  );
                })}
              </div>
              <div class="flex items-center justify-between pt-2">
                <p class="text-sm text-gray-500">{proyecto.estado}</p>
                {proyecto.link && (
                  <a 
                    href={proyecto.link} 
                    class="bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 transition font-medium text-sm inline-flex items-center gap-2"
                  >
                    Ver proyecto
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                    </svg>
                  </a>
                )}
              </div>
            </div>
          </div>
        );
      })}
    </div>

    <div class="bg-blue-50 rounded-lg p-8 border-l-4 border-blue-600">
      <h2 class="text-2xl font-bold text-gray-900 mb-4">Interesado en colaborar?</h2>
      <p class="text-gray-700 mb-6">
        Siempre estoy abierto a nuevas colaboraciones y proyectos interdisciplinarios. 
        Si tienes una idea o propuesta que te gustaría discutir, no dudes en contactarme.
      </p>
      <a href={`${base}/contacto`} class="bg-blue-600 text-white px-6 py-3 rounded-lg hover:bg-blue-700 transition font-medium inline-block">
        Ponte en contacto
      </a>
    </div>
  </div>
</Layout>
